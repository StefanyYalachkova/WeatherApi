phrase:
  project_id: 75a88f61383e69ddb18c106dd1222dfc

  push:
    sources:
      - file: ./<locale_name>.json
        params:
          file_format: simple_json
          locale_id: en

    pull:
    targets:
      - file: ./<locale_name>.json
        params:
          file_format: simple_json
